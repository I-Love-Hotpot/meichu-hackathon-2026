{
  "name": "cloudphone-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "sync:medicines": "node scripts/sync-medicines.js",
    "dev": "node --env-file-if-exists=.env --watch src/app.js",
    "start": "node --env-file-if-exists=.env src/app.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "fastify": "^5.0.0",
    "twilio": "^6.1.1"
  }
}
