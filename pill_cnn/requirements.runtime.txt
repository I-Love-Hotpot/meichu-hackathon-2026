# Keep the native wheels compatible with hosts that expose only the baseline
# x86-64 instruction set (for example older KVM virtual CPUs).
numpy==1.26.4
opencv-python==4.10.0.84
pandas==2.2.3
pillow==10.4.0
PyYAML==6.0.2
ultralytics==8.3.0
