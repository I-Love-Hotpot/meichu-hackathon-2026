model:
  detector_weights: models/pill_detector.pt
  device: cpu
  image_size: 640
  confidence_threshold: 0.25
  low_confidence_threshold: 0.01
  iou_threshold: 0.7

matching:
  top_k: 3

classifier:
  model_weights: models/cnn/pill_classifier.pt

database:
  path: database/42_2.csv
